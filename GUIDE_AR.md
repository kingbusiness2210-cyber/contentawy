# إزاي تشغل تطبيق كونتنتاوي (Contentawy) على موبايلك بسهولة؟ 📱✨

أهلاً بيك يا بطل! الدليل ده معمول مخصوص ليك بالعربي المصري، ومكتوب بأسلوب بسيط جداً بدون أي مصطلحات برمجية معقدة، وبدون ما تحتاج تثبت أي برامج أو Flutter على كمبيوترك.

---

## 🎯 مسار التسليم المباشر (GitHub Actions Cloud Build)

بما إن جهازك مش متثبت عليه بيئة Flutter أو Java، جهزنا لك نظام **Cloud Build مجاني 100%** في ملف `.github/workflows/android-build.yml`.
السحابة (سيرفرات GitHub) هتعمل Build للتطبيق وتطلعلك ملف `app-release.apk` جاهز للتحميل والتثبيت في ثواني!

---

### 1️⃣ الخطوة الأولى: رفع المشروع على GitHub (بضغطة واحدة)

1. ادخل على موقع [GitHub.com](https://github.com) واعمل تسجيل دخول (أو حساب مجاني لو معندكش).
2. اضغط على علامة **`+`** فوق على اليمين واختار **New repository**.
3. اكتب اسم المستودع: `contentawy`، وخلي المستودع **Public** أو **Private** براحتك، ثم اضغط **Create repository**.
4. افتح موجه الأوامر (Terminal أو PowerShell) في فولدر المشروع، وشغل الأوامر التلاتة دول بس (أو شغل ملف `push_to_github.bat` مباشرة):

```bash
git init
git add .
git commit -m "إطلاق تطبيق كونتنتاوي الإصدار الأول"
git branch -M main
git remote add origin https://github.com/اسم_حسابك/contentawy.git
git push -u origin main
```

---

### 2️⃣ الخطوة الثانية: تنزيل ملف الـ APK من GitHub Actions

1. افتح صفحة الـ Repo بتاعك على GitHub من المتصفح.
2. اضغط على تبويب **Actions** من الشريط العلوي.
3. هتلاقي الـ Workflow شغال باسم: `Build Contentawy Android APK` وجنبه دايرة صفراء بتلف 🔄.
4. استنى دقيقتين لحد ما تظهر علامة صح خضراء `Done` ✅.
5. اضغط على اسم الـ Workflow، وانزل لتحت عند قسم **Artifacts**.
6. اضغط على: **`Contentawy-Release-APK`** وهيبدأ تحميل ملف الـ APK فوراً على جهازك! 🚀

---

### 3️⃣ الخطوة الثالثة: إرسال الـ APK للموبايل وتثبيته

تقدر تبعت الملف لموبايلك بأي طريقة من دول:
- **واتساب (WhatsApp Web):** ابعت الملف لنفسك في محادثة "You" أو لأي صديق وحمله على الموبايل.
- **تليجرام (Telegram):** في خانة "Saved Messages" ابعت الملف وافتحه مباشرة.
- **جوجل درايف (Google Drive):** ارفع الملف ونزله من تطبيق درايف على الموبايل.
- **كابل USB:** وصل الموبايل بالكمبيوتر وانقل ملف الـ APK لفولدر `Download`.

---

### 4️⃣ الخطوة الرابعة: التثبيت على الأندرويد (تخطي تحذير Unknown Apps)

1. اضغط على ملف الـ `app-release.apk` على موبايلك.
2. لو ظهرت رسالة: **"For your security, your phone is not allowed to install unknown apps"**:
   - اضغط على **Settings (الإعدادات)**.
   - فعل خيار: **Allow from this source (السماح من هذا المصدر)**.
   - ارجع واضغط **Install (تثبيت)**.
3. لو ظهر فحص حماية Play Protect، اضغط **Install anyway (التثبيت على أي حال)**.
4. مبروك! 🎉 اضغط **Open (فتح)** واستمتع بالتطبيق!

---

## 💾 ميزة النسخ الاحتياطي والـ Offline First

- **البيانات محفوظة على جهازك:** مفيش أي بيانات بتروح لسيرفرات خارجية أو تتبع.
- **نقل البيانات لهاتف جديد:**
  1. افتح **الإعدادات** في التطبيق.
  2. اضغط على **تصدير نسخة احتياطية 💾**.
  3. هيتم نسخ كود الـ JSON، احفظه عندك في الملاحظات أو الواتساب.
  4. على الموبايل الجديد، افتح الإعدادات واضغط **استرجاع نسخة 📥** والصق الكود.

---

## 🛠️ أهم ملفات المشروع:

- **الأيقونات واللوجو:** داخل `assets/icons/` و `android/app/src/main/res/mipmap-*`
- **قاعدة البيانات والتخزين المحلي:** `lib/core/services/storage_service.dart`
- **النسخ الاحتياطي:** `lib/core/services/backup_service.dart`
- **الحاسبات التسويقية:** `lib/core/utils/marketing_math.dart`
- **واجهات التطبيق:** `lib/screens/`
- **إعدادات البناء السحابي:** `.github/workflows/android-build.yml`
